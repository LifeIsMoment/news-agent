# 프로젝트 적용성·자동 개선 운영 원칙

이 저장소의 최종 목적은 TAXiA·CLOA 언급량을 늘리는 것이 아니라, 공개 정보에서 **프로젝트에 적용할 수 있는 변화**를 추출하고 구현·검증 가능한 작업으로 전환하는 것이다.

## 매일 생성되는 산출물

- 원시 수집 보고서: `reports/`, `reports-finance-tax-ai/`
- 프로젝트 적용 브리프: `applications/YYYY/MM/YYYY-MM-DD.md`
- 기계 판독 구현 패킷: `applications/YYYY/MM/YYYY-MM-DD.json`
- 품질·노이즈·소스 상태: `quality/YYYY/MM/YYYY-MM-DD.json`
- 실행 이슈: `[프로젝트 적용] YYYY-MM-DD 모니터링 브리프`

각 적용 항목에는 다음을 포함한다.

1. 적용성 점수와 증거 신뢰도
2. 대상 프로젝트 구성요소와 우선 확인 파일
3. 구체적인 변경 제안
4. 구현 순서
5. 완료·인수 기준
6. 자동화 한계와 사람 검토가 필요한 부분

## 프로젝트 매핑

`config/project-context.json`은 TAXiA의 공개 구조를 기준으로 다음 영역을 연결한다.

- 세법 지식 수집·버전·인덱싱
- 인용·답변 안전성
- 개인정보·감사추적
- 데이터 출처·저작권·라이선스
- 보안·신뢰성·운영
- SDK·API·배포
- 평가·회귀·벤치마크
- 문서·회계·공공 API 통합
- 제품·시장·사업정책

## 자동 개선의 경계

자동화는 매일 결과를 평가하고 다음을 수행한다.

- 오탐, 기준선, 자기참조, 오래된 항목을 적용 목록에서 억제
- 적용성·신뢰도 임계값을 설정된 상하한 안에서 자동 조정
- 수집 결과를 파일·작업·완료 기준이 있는 구현 패킷으로 변환
- 알려진 오탐 패턴에는 allowlist 코드 패치와 회귀 테스트를 생성
- 테스트, 변경 파일 allowlist, diff 크기 제한을 통과한 수집기 품질 패치만 자동 병합 시도
- GitHub Actions 의존성 업데이트 PR을 Dependabot으로 생성

다음은 자동 병합하지 않는다.

- GitHub Actions 권한 확대
- secrets 또는 외부 전송 대상 변경
- 워크플로 파일 자체 변경
- 법적 판단이나 세무 결론의 확정
- `xaikorea/taxia` 프로젝트 소스 변경

현재 연결 계정은 `xaikorea/taxia`에 읽기 권한만 있다. 따라서 실제 프로젝트 코드 변경은 정확한 파일, 구현 순서, 완료 기준을 포함한 패킷으로 생성되며, 대상 저장소 쓰기 권한이 연결되면 PR 자동화로 확장할 수 있다.
